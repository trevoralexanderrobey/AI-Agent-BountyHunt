---
name: {{ slug }}
description: {{ description_json }}
type: {{ skill_type }}
---

# {{ tool_name }} Skill

Generated by `spawner.py` for OpenClaw Bridge runtime integration.

## Mode

- Type: `{{ skill_type }}`
{% if is_gui %}
- Runtime: Local GUI daemon bridge at `{{ daemon_url }}`
{% else %}
- Runtime: Headless Docker wrapper (`kali-rolling`)
{% endif %}

## Tools

{% if is_gui %}
- `{{ slug }}_health`
- `{{ slug }}_session_start`
- `{{ slug }}_session_status`
- `{{ slug }}_session_close`
{% else %}
- `{{ slug }}_run`
- `{{ slug }}_health`
{% endif %}

## TSP Safety

This generated skill embeds a simplified Traffic Summarization Protocol:

- Redacts sensitive headers: `Authorization`, `Cookie`, `Set-Cookie`, `X-API-Key`
- Truncates large outputs to preserve token context

{% if net_sensitive %}
## Network Caveat

- `{{ tool_name }}` may require host-level network visibility for local service testing.
{% if inject_host_net %}
- On Linux, this generated skill injects `--net=host` for Docker execution.
{% elif macos_warning %}
- On macOS, Docker Desktop network isolation can limit visibility into host-local services.
{% else %}
- If needed, regenerate with platform-appropriate networking options.
{% endif %}
{% endif %}

## Metadata

- Slug: `{{ slug }}`
- Generated: `{{ generated_at_iso }}`
